type Step {
    id: Int!
    parentStep: Int
    priority: Int!
    title: String!
    description: String!
    completed: Boolean!
    generation: Int
    hasChild: Boolean!
}

type FullStep {
    details: Step!
    resources: [Resource]
    subSteps: [FullStep]
}

