type Mutation {
    generateCourse(description: String): FullCourse
}

type Mutation {
    getCourseById(courseId: Int!): FullCourse
}

type Mutation {
    accessCourse(courseId: Int!): Course
}

type Mutation {
    completeCourse(courseId: Int!): Course
}

type Mutation {
    changePublishDetails(courseId: Int!, title: String, description: String, tags: [String]): Course
}

type Mutation {
    publishCourse(courseId: Int!): Course
}