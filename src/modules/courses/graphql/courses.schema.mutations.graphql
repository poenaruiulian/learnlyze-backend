type Mutation {
    generate (description: String): FullCourse
}

type Mutation {
    getFullById(courseId: Int!): FullCourse
}

type Mutation {
    access(courseId: Int!): Course
}

type Mutation {
    complete(courseId: Int!): Course
}

type Mutation {
    changePublishDetails(courseId: Int!, title: String, description: String, tags: [String]): Course
}

type Mutation {
    publish(courseId: Int!): Course
}

type Mutation {
    enroll(courseId: Int!): Course
}