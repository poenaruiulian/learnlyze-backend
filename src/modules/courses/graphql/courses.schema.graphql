type Course {
    id: Int!
    user: Int!
    title: String!
    description: String
    tag: String
    startedAt: String!
    lastAccessed: String!
    postedDate: String
    completedSteps: Int!
    steps: [Int]
}

type FullCourse {
    details: Course!
    steps: [FullStep]
}